// arrays, objetos
let meta = {
  value: 'ler um livro por mês',
  checked: false,
  isChecked: (info) => {console.log(info)}
}

//console.log(meta.value);
meta.isChecked(meta.value);

// function // arrow function
const criarMeta = () => {}

// named function
// function criarMeta() {}